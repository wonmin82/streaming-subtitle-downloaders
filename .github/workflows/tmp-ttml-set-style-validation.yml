name: Temporary TTML set styling validation
on:
  pull_request:
    types: [reopened]
    branches: [main]
permissions:
  contents: write
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: validate/coupang-ttml-set-style
          fetch-depth: 0
      - run: python3 .github/tmp/patch-ttml-set-style.py
      - run: |
          node --check scripts/coupang-play-subtitles-downloader.user.js
          git diff --check
      - run: node .github/tmp/test-ttml-set-style.js
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/coupang-play-subtitles-downloader.user.js
          git commit -m "Validate TTML set styling implementation"
          git push origin HEAD:validate/coupang-ttml-set-style
