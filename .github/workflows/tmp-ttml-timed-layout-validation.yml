name: Temporary TTML timed layout validation
on:
  pull_request:
    types: [opened]
    branches: [main]
permissions:
  contents: write
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: validate/coupang-ttml-timed-layout
          fetch-depth: 0
      - run: python3 .github/tmp/patch-ttml-timed-layout.py
      - run: |
          node --check scripts/coupang-play-subtitles-downloader.user.js
          git diff --check
      - run: node .github/tmp/test-ttml-timed-layout.js
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/coupang-play-subtitles-downloader.user.js
          git commit -m "Validate timed TTML layout implementation"
          git push origin HEAD:validate/coupang-ttml-timed-layout
